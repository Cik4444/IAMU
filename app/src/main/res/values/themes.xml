<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.BoxingApp" parent="Theme.Material3.DayNight">
        <!-- Jetpack Compose handles the app bar, so disable the ActionBar -->
        <item name="windowActionBar">false</item>
        <item name="android:windowNoTitle">true</item>
    </style>
</resources>
